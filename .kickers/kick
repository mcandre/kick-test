#!/bin/bash
unset IFS
set -eufo pipefail

#
# Sync changes between local and remote for current git branch
#

git fetch --all --tags

git add .

if [ -n "$(git status --porcelain)" ]; then
    git commit -m 'up'
    git pull
    git push
fi

git push --tags
